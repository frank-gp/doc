```sh
where sam

# Abre el archivo .bashrc:
code ~/.bashrc

# Añade esta línea al final:
alias sam='sam.cmd'

# Guarda el archivo y recarga la configuración:
source ~/.bashrc

sam --version
