<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <header>
        <h1><%= title %></h1>
    </header>
    
    <main>
        <% if (tasks.length === 0) { %>
            <p>Empty tasks</p>
        <% } else { %>
            <ul>
                <% tasks.forEach(task => { %>
                    <li><%= task.title %></li>
                <% }); %>
            </ul>
        <% } %>
    </main>

    <footer>
        <p>&copy; 2023 Your Company</p>
    </footer>
</body>
</html>
