# DevTools

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-devtools</artifactId>
    <scope>runtime</scope>
</dependency>
```

```sh
mvn spring-boot:run

# Alternativa con recompilación continua (modo terminal puro)
mvn compile -Dcompiler.watch

```
