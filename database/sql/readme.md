# Postgres

```sh
# Show version
psql --version

# Open Postgres
psql -U postgres

# AWS
psql -U postgres -h database-2-postgres.asdf.us-east-2.rds.amazonaws.com -p 5432 -d postgres

exit
```

# MySql

```sh
# Where mysql
where mysql
# C:\laragon\bin\mysql\mysql-8.0.30-winx64\bin
# C:\xampp\mysql\bin

# Show version
mysql --version

# Open the MySQL
mysql -u root

# AWS
mysql -u admin -h database-1.asdf.us-east-2.rds.amazonaws.com -P 3306 -p


# Clear the screen
system clear
system cls

# run file sql
mysql -u root -p < db.sql

exit
```
